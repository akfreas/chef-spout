DATABASES = {
    'default': {
    'ENGINE': 'django.db.backends.<%= @database[:settings]['engine'] %>', # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': '<%= @database[:settings]['database'] %>',                      # Or path to database file if using sqlite3.
        'USER': '<%= @database[:settings]['username'] %>',                      # Not used with sqlite3.
        'PASSWORD': '<%= @database[:settings]['password'] %>',                  # Not used with sqlite3.
        'HOST': 'localhost',                      # Set to empty string for localhost. Not used with sqlite3.
        'PORT': '5432',                      # Set to empty string for default. Not used with sqlite3.
    }
}
